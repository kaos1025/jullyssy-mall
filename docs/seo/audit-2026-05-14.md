# SEO Foundation Audit — 2026-05-14

- 브랜치: `feat/seo-foundation`
- audit 범위: Phase 1 SEO 인프라 회귀 + Day 9~18 신규 라우트 통합 점검
- 직전 상태: main `48de10f fix(legal): 배송비 정책 표기 SHIPPING_CONFIG와 정합화 (#13)` (Day 18 결제 보안 작업 직후 안정)
- v14 S0-1 풀스코프 진입 전 사전 작업 — 코드 변경 없음(audit only)

---

## A. 메타데이터 커버리지

| 라우트 | metadata | noindex 필요 | 상태 | 조치 |
|--------|----------|--------------|------|------|
| `/` (`(shop)/page.tsx`) | static, canonical `/` | – | ✅ | – |
| `/products` (list) | dynamic by `?category`, canonical | – | ✅ | – |
| `/products/[id]` | dynamic, OG, keywords, canonical | – | ✅ | – |
| `/events/[id]` Day 12 | dynamic title, canonical, **OG 없음** | – | ⚠️ | OG image/description 추가(P2) |
| `/mypage/reviews` Day 9 | static `리뷰 관리 \| 쥴리씨` | **필요** | ⚠️ | `robots: { index: false }` 누락 |
| `/mypage/reviews/write/[orderItemId]` Day 9 | static `리뷰 작성 \| 쥴리씨` | **필요** | ⚠️ | `robots: { index: false }` 누락 |
| `/mypage/orders` | static `주문내역 \| 쥴리씨` | **필요** | ⚠️ | `robots: { index: false }` 누락 |
| `/mypage/(with-sidebar)/{addresses,coupons}` | – | **필요** | ⚠️ | metadata + noindex 누락 |
| `/mypage` (redirect) | – | **필요** | ⚠️ | redirect 페이지 — 영향 작음(P2) |
| `/cart` | – ("use client" page) | **필요** | 🔴 | meta noindex 부재. robots.ts disallow에만 의존 |
| `/checkout` | – ("use client" page) | **필요** | 🔴 | meta noindex 부재 |
| `/order-complete` | – | **필요** | ⚠️ | 주문 결과 페이지, noindex 누락 |
| `/admin/**` (전 라우트) | – | **필요** | 🔴 | admin layout/page 모두 noindex 부재 |
| `/privacy` PR #13 | static, **noindex 설정됨** | – | ⚠️ | 법적 고지(개인정보처리방침)는 일반적으로 index 권장 — 신뢰 시그널 |
| `/terms` | static, **noindex 설정됨** | – | ⚠️ | 동일 — index 권장 |

> 보충: noindex 부재는 `robots.ts`의 `Disallow:`로 부분 보완됨. 하지만 외부 링크로 유입된 페이지는 크롤 차단만으로는 색인을 막지 못함 → meta `noindex` 병행이 표준. 다만 `mypage/checkout/cart/admin`은 비로그인/세션 의존 라우트라 실 색인 위험은 낮음.

> "use client" 컴포넌트인 `/cart`, `/checkout`은 `generateMetadata`를 직접 export 불가. 동일 디렉토리 `layout.tsx`(서버) 신설 또는 별도 metadata 분리 패턴 필요.

---

## B. JSON-LD 회귀

| 위치 | 스키마 | 상태 | 메모 |
|------|--------|------|------|
| `(shop)/page.tsx:60-100` | WebSite + Organization + SearchAction | ✅ | 홈에 출력. 루트 layout 이상이 더 안전하지만 동작은 정상 |
| `/products/[id]:204-241` | Product (brand/offers/seller) | ✅ | – |
| `/products/[id]:233-241` | aggregateRating (조건부 — 리뷰 ≥1) | ✅ | reviews 집계 기반, products 테이블 review_count/avg 별도 불필요 |
| `/events/[id]` | (없음) | ⚠️ | ItemList 또는 CollectionPage 미적용 |
| Breadcrumb (`@type: BreadcrumbList`) | (없음) | 📌 P2 | PDP에 카테고리 텍스트 breadcrumb은 있으나 JSON-LD 미출력 |

---

## C. Sitemap 커버리지 ⭐ #48 진입 트리거

`src/app/sitemap.ts` 분석:

| 항목 | 상태 | 비고 |
|------|------|------|
| `/`, `/products`, `/login`, `/signup` | ✅ | 정적 4건 포함 |
| `/products?category={slug}` | ✅ | 48개 카테고리 전수 (slug 100% populated 확인) |
| `/products/{slug \|\| id}` (active product) | 🔴 | **slug 100% null — 41/41건 UUID URL 노출** |
| `/events/{id}` (active event) Day 12 | 🔴 | **sitemap에서 누락** (active_events=1) |
| `/categories` | ⚠️ | 명시적 `/categories` 경로 없음(목록은 `/products?category=`로 처리) — 사양상 이슈 없음 |
| `/terms`, `/privacy` | ⚠️ | 미포함 — 다만 현 시점 noindex라 OK. index로 전환 시 sitemap 추가 권장 |
| `/admin/*`, `/mypage/*`, `/checkout`, `/cart`, `/auth/*`, `/api/*` 제외 | ✅ | sitemap 미생성 |

> Supabase 실측: `SELECT COUNT(*) FROM products WHERE status='ACTIVE'` → **41건**, `COUNT(slug)` → **0건**, 커버리지 **0.0%**.
> 카테고리 slug 커버리지: 48/48 ✅. 이벤트 카테고리 active 1건.

### #48 진입 결정
- 상품 URL 패턴: **100% UUID 노출** → `/products/<UUID>` 그대로 사이트맵/EP feed에 들어가는 중
- → **#48 즉시 진입 불가**. **#48-2 slug backfill을 P1 신규 등록 + 선행 필요**.
- 백필 후 EP feed/SC 제출/사이트맵 재생성 순서 권장.

---

## D. robots.txt

`src/app/robots.ts` 분석:

| 항목 | 상태 | 메모 |
|------|------|------|
| Disallow `/admin`, `/api`, `/mypage`, `/checkout`, `/cart` | ✅ | – |
| Disallow `/auth` | ⚠️ | 누락. `(auth)/login`, `(auth)/signup`은 라우트 그룹 — 실제 URL은 `/login`, `/signup`이라 일반적으로 index 가능. spec상 누락이긴 하나 영향 작음 |
| Sitemap URL 명시 | ✅ | `${SITE_URL}/sitemap.xml` |
| baseUrl 패턴 | ⚠️ | `process.env.NEXT_PUBLIC_SITE_URL \|\| "http://localhost:3000"` — `BUSINESS_INFO.siteUrl` 미사용. 일관성 떨어짐(constants/business.ts 사용 권장) |

---

## E. 이미지 alt

| 컴포넌트 | alt 패턴 | 상태 |
|---------|---------|------|
| `components/product/ProductCard.tsx:115,125` | `{name}`, `{name} hover` | ✅ 동적 |
| `components/product/ImageGallery.tsx:43,122` | `상품 이미지 {N}`, `썸네일 {N}` | ⚠️ 상품명 미포함 |
| `components/product/ProductOptions.tsx:401` | `{productName}` | ✅ |
| `components/product/ReviewFormBody.tsx:263` | `첨부 이미지 {N}` | ✅ (사용자 폼) |
| `components/home/HeroBanner.tsx:111,122` | `{s.title}` | ✅ |
| `components/banners/TopBannerCarousel.tsx` Day 11 | – (텍스트 배너 only, 이미지 없음) | N/A |
| `components/review/MiniReviewCarousel.tsx:49` Day 9 | `{writerName}님 리뷰 이미지` | ✅ |
| `components/review/ReviewCard.tsx:111` | `{masked}님 리뷰 이미지` | ✅ |
| `components/event/*` Day 12 | – (이벤트 페이지는 ProductCard 재사용, 별도 이미지 없음) | N/A |
| `components/layout/NaverImportButton.tsx:256` | `""` (장식 이미지, 의도적) | ✅ |

> 종합: 핵심 노출 경로(PDP, 카드, 배너, 리뷰)는 동적 alt. ImageGallery만 상품명 prop 받아서 alt에 포함하면 더 좋음(P2).

---

## F. 시맨틱 HTML — h1 위계

| 라우트 | h1 | 상태 |
|--------|----|------|
| `/` | `<h1 className="sr-only">쥴리씨 여성의류 쇼핑몰</h1>` | ✅ 1개 |
| `/products` | `<h1>{title}</h1>` | ✅ 1개 |
| `/products/[id]` | `<h1>{product.name}</h1>` | ✅ 1개 |
| `/events/[id]` Day 12 | `<h1>{emoji}{category.name}</h1>` | ✅ 1개 |
| `/mypage/*` (with-sidebar) | layout h1 `마이페이지` only, 자식 페이지 h1 없음 | ✅ 1개 |
| `/mypage/reviews/write/[orderItemId]` (focused) Day 9 | `<h1 className="jl-h1">리뷰 작성</h1>` | ✅ 1개 |
| `/cart`, `/checkout`, `/order-complete` | 각 자체 h1 | ✅ 1개 |
| `/privacy`, `/terms` | 각 자체 h1 | ✅ 1개 |
| `/admin/**` | **AdminLayout h1 `쥴리씨 관리자` + 각 page h1 (대시보드/주문 관리/…)** | ⚠️ 2개 |

> 시맨틱 요소: `<main>`, `<nav>`, `<header>`, `<footer>` 모두 (shop)/layout.tsx와 admin/layout.tsx에서 사용 ✅.
> admin h1 중복은 noindex 대상이라 SEO 영향 낮음 — P2.

---

## G. Canonical URL

| 라우트 | canonical 패턴 | 상태 |
|--------|---------------|------|
| `/` | `"/"` | ✅ |
| `/products` (no filter) | `"/products"` | ✅ |
| `/products?category=X` | `"/products?category=X"` | ✅ self-canonical |
| `/products?category=X&sort=Y&page=Z` | category만 반영, sort/page 무시(self-canonical to category base) | ✅ 의도된 동작 — sort/page 페이지네이션 변형은 canonical로 통합 |
| `/products/[id]` | `/products/${slug \|\| id}` | ✅ — slug 우선 |
| `/events/[id]` Day 12 | `/events/${category.id}` | ✅ |
| `/privacy`, `/terms`, `/cart`, `/checkout`, `/mypage`, `/admin` | 없음 | ⚠️ noindex 대상이라 일반적으로 OK |
| 루트 자동 canonical | 패턴 없음 — 라우트별 명시 출력 | ⚠️ generateMetadata 미작성 라우트는 canonical 비출력. metadataBase는 layout.tsx에 설정됨 ✅ |

---

## H. Verification slot ⭐ #46/#47 진입 사전 확인

`src/app/layout.tsx:36-39`:

```ts
verification: {
  // 네이버 서치어드바이저 등록 후 추가
  // other: { "naver-site-verification": "인증코드" },
},
```

| 항목 | 상태 |
|------|------|
| `verification.other['naver-site-verification']` | 🔴 슬롯 없음 (주석만 존재) |
| `verification.google` | 🔴 슬롯 없음 |
| `NEXT_PUBLIC_NAVER_SITE_VERIFICATION` env | 🔴 `.env.local.example` 미정의 |
| `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` env | 🔴 미정의 |

### #46/#47 작업 형태 결정
- 결과: **slot/env 둘 다 없음** → **layout.tsx에 verification slot 신설 + .env.example 갱신** 필요.
- 본 audit 세션에서는 코드 변경 없음(spec). 다음 Phase에서 4~6줄 코드 변경 + env 2건 추가로 종결 가능.

---

## 발견 항목 요약

- 🔴 즉시 fix 권장 (≤30m): **4건**
  - C-1: 상품 slug 100% null → #48-2 slug backfill 선행 (별도 작업 아님 → P1 등록 권장)
  - C-2: `/events/[id]` sitemap 누락
  - A-1: `/cart`, `/checkout` noindex 부재 (use client 페이지 layout 패턴 적용)
  - A-2: `/admin/**` noindex 부재 (admin/layout.tsx에 `export const metadata = { robots: { index: false } }`)
- ⚠️ 즉시 fix 가능 (≤1h): **6건**
  - A-3: `/mypage/**` 페이지/레이아웃 noindex 부재
  - A-4: `/order-complete` noindex 부재
  - A-5: `/privacy`, `/terms` noindex 해제 검토(법적 고지는 일반적으로 index)
  - B-1: `/events/[id]` ItemList(또는 CollectionPage) 스키마 추가
  - D-1: robots.ts baseUrl을 `BUSINESS_INFO.siteUrl` 정렬
  - H-1: layout.tsx verification slot 신설 + env 정의
- 📌 P1 별도 등록: **2건**
  - **#48-2 product slug backfill** (#48 EP feed 진입 전제) — 41/41 backfill SQL + import 시 slug 자동 생성 정책
  - **#46/#47 Verification 코드 추가** (slot 신설 + env)
- 📌 P2 분리: **5건**
  - A-6: `/mypage` redirect 페이지 noindex
  - B-2: PDP Breadcrumb JSON-LD
  - E-1: ImageGallery alt에 상품명 포함
  - F-1: admin h1 위계 정리 (2개 → 1개)
  - G-1: 카테고리 sort/page 변형 canonical 명시 패턴 문서화
- ✅ 정상: **다수**
  - PDP Product/aggregateRating, 홈 WebSite/Organization JSON-LD, 카테고리 slug 100%, 핵심 컴포넌트 alt, h1 단일성(non-admin), 핵심 라우트 canonical, robots.ts Disallow/Sitemap URL

---

## #48 진입 결정 트리거 (C 결과 기반)

- 상품 URL 패턴: **UUID (100%)** — `41/41 active products have NULL slug`
- → **#48 EP feed 즉시 진입 불가**. **#48-2 slug backfill을 P1 신규 등록 후 진행**
- 백필 완료 시점에 한해 sitemap 재생성 + 네이버 EP feed/Google SC 제출

---

## PM 의사결정 요청

1. **#48-2 slug backfill P1 등록 승인** — 41건 backfill SQL + Naver import 시 slug 자동 생성 정책. `#48` 본 작업의 차수석 의존성으로 묶음 처리할지, 아니면 별 P1로 분리할지 결정.
2. **Phase 2 즉시 fix 4건 진입 승인** — 🔴 4건(C-2 events sitemap / A-1 cart·checkout / A-2 admin noindex)을 이번 세션에서 동일 브랜치(`feat/seo-foundation`)로 처리할지, 별 브랜치로 분리할지 결정. C-1은 P1 분리.
3. **Phase 3 #46/#47 verification 작업 진입 승인** — H-1을 동일 브랜치에서 처리. layout.tsx 4~6줄 + env 2건. 인증코드는 별도 발급 후 .env에 주입.
4. **/privacy, /terms noindex 정책 결정** — 법적 고지는 일반적으로 index가 표준이지만, PIPA 갱신 직후 노출 부담이 있다면 단기 noindex 유지 가능. PM 판단.

---

## 부록 — 검증 명령

```bash
# 상품 slug 커버리지 재확인
SELECT COUNT(*) AS total, COUNT(slug) AS with_slug
FROM products WHERE status = 'ACTIVE';

# 활성 이벤트 카테고리
SELECT COUNT(*) FROM event_categories WHERE is_active = true;
```

검증 시점: 2026-05-14, project_id `mdtvnbyvhzhbksssyzgl` (jullyssy-mall).
